# How it works

!!! warning "Not written yet"
    **Question this page answers:** why does the distance between two embeddings mean
    "something changed here", and what follows from that?

    One essay, read in order: embeddings → the change score → coverage → cells → objects →
    the classifier. Harvested from the module docstrings in `embed_cd/`.
