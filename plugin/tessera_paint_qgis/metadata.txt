[general]
name=TESSERA Paint
qgisMinimumVersion=3.28
qgisMaximumVersion=4.99
description=Click a pixel, highlight everything similar using TESSERA embeddings, save a mask.
about=Explore precomputed TESSERA per-pixel Earth embeddings inside QGIS: click a pixel to highlight all similar pixels (cosine similarity) and export the selection as a mask. CPU-only, no model runs locally. Requires geotessera, numpy, and rasterio installed into the QGIS Python (OSGeo4W shell: pip install geotessera numpy rasterio).
version=0.14.0
author=Alex Graham
email=alexgraham9966@gmail.com
tracker=https://example.invalid/todo
repository=https://example.invalid/todo
experimental=True
deprecated=False
tags=remote sensing,embeddings,tessera,similarity,segmentation,mask
category=Raster
