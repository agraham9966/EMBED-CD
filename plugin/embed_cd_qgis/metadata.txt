[general]
name=EMBED-CD
qgisMinimumVersion=3.28
qgisMaximumVersion=4.99
description=Change maps from satellite embeddings. Draw an area, pick two years, classify what changed.
about=Generates year-over-year land change maps from AlphaEarth satellite embeddings (Google Satellite Embedding V1), which cover the whole globe for every year 2017-2025 at 10 m. Works tile by tile and fills the canvas as each tile completes, so memory stays flat and large areas are possible. Missing data is shown as its own class, never as "no change". CPU-only, nothing to train, no account or API key — data is read directly from public cloud-optimized GeoTIFFs. No installation steps: everything it needs (GDAL, numpy, scipy, pyarrow) already ships with QGIS. Tested on QGIS 4.0.1; 3.28+ should work but is untested. The AlphaEarth Foundations Satellite Embedding dataset is produced by Google and Google DeepMind (CC-BY 4.0).
version=0.14.0
author=Alex Graham
email=alexgraham9966@gmail.com
tracker=https://example.invalid/todo
repository=https://example.invalid/todo
experimental=True
deprecated=False
tags=remote sensing,change detection,alphaearth,embeddings,sentinel,landsat
category=Raster
